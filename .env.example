# PostgreSQL (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/form_builder"

# Random secret for cookie sessions — generate with: openssl rand -base64 32
SESSION_SECRET="change-me-in-production"

# OpenAI API key (required only for spec 05 — AI agent bonus feature)
OPENAI_API_KEY="sk-..."

NODE_ENV="development"
